/**
 * Jest Configuration — Backend (NestJS)
 *
 * Stack: NestJS + TypeScript
 * Transform: SWC (@swc/jest)
 *
 * Generated by New-Project-Starter-Kit
 */

import type { Config } from 'jest';

const config: Config = {
  displayName: '{{PROJECT_NAME}}-backend',
  testEnvironment: 'node',

  // ---------------------------------------------------------------------------
  // Transform — SWC
  // ---------------------------------------------------------------------------
  transform: {
    '^.+\\.(t|j)s$': ['@swc/jest', {
      jsc: {
        target: 'es2021',
        parser: {
          syntax: 'typescript',
          decorators: true,
        },
        transform: {
          decoratorMetadata: true,
          legacyDecorator: true,
        },
      },
    }],
  },

  // ---------------------------------------------------------------------------
  // Module resolution
  // ---------------------------------------------------------------------------
  moduleNameMapper: {
    '^@/(.*)$': '<rootDir>/src/$1',
  },

  // Custom resolver to fix SWC resolving @/ aliases before moduleNameMapper
  resolver: '<rootDir>/jest-resolver.cjs',

  // ---------------------------------------------------------------------------
  // Test matching
  // ---------------------------------------------------------------------------
  testRegex: '.*\\.spec\\.ts$',
  roots: ['<rootDir>/src/'],

  // ---------------------------------------------------------------------------
  // Setup
  // ---------------------------------------------------------------------------
  setupFilesAfterSetup: ['<rootDir>/{{TEST_SETUP_PATH}}'],

  // ---------------------------------------------------------------------------
  // Module file extensions
  // ---------------------------------------------------------------------------
  moduleFileExtensions: ['js', 'json', 'ts'],

  // ---------------------------------------------------------------------------
  // Coverage
  // ---------------------------------------------------------------------------
  collectCoverageFrom: [
    'src/**/*.ts',
    '!src/**/*.d.ts',
    '!src/**/*.spec.ts',
    '!src/**/*.module.ts',
    '!src/main.ts',
    '!src/**/*.dto.ts',
    '!src/**/*.entity.ts',
    '!src/**/*.interface.ts',
  ],
  coverageThreshold: {
    global: {
      branches: {{COVERAGE_BRANCHES}},
      functions: {{COVERAGE_FUNCTIONS}},
      lines: {{COVERAGE_LINES}},
      statements: {{COVERAGE_STATEMENTS}},
    },
  },
  coverageDirectory: '<rootDir>/coverage',
  coverageReporters: ['text', 'text-summary', 'lcov', 'clover'],
};

export default config;
