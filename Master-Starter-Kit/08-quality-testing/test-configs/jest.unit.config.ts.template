/**
 * Jest Configuration — Unit Tests Only
 *
 * Extends the main config but excludes E2E/integration tests.
 * Optimized for fast CI runs and pre-commit hooks.
 *
 * Usage:
 *   {{PKG_MANAGER}} test -- --config jest.unit.config.ts
 *
 * Generated by New-Project-Starter-Kit
 */

<!-- IF {{APP_TYPE}} == "frontend" -->
import type { Config } from 'jest';
import baseConfig from './jest.config';

const config: Config = {
  ...baseConfig,
  displayName: '{{PROJECT_NAME}}-unit',

  // ---------------------------------------------------------------------------
  // Exclude E2E / integration tests
  // ---------------------------------------------------------------------------
  testPathIgnorePatterns: [
    '<rootDir>/node_modules/',
    '<rootDir>/e2e/',
    '<rootDir>/tests/e2e/',
    '<rootDir>/src/**/*.e2e.{ts,tsx}',
    '<rootDir>/src/**/*.integration.{ts,tsx}',
  ],

  // Only match unit test files
  testMatch: [
    '<rootDir>/src/**/*.test.{ts,tsx}',
    '<rootDir>/src/**/*.spec.{ts,tsx}',
  ],

  // ---------------------------------------------------------------------------
  // Performance optimizations for CI
  // ---------------------------------------------------------------------------
  maxWorkers: '50%',
  bail: 1,
  verbose: false,
};

export default config;
<!-- ENDIF -->

<!-- IF {{APP_TYPE}} == "backend" -->
import type { Config } from 'jest';
import baseConfig from './jest.config';

const config: Config = {
  ...baseConfig,
  displayName: '{{PROJECT_NAME}}-unit',

  // ---------------------------------------------------------------------------
  // Exclude E2E / integration tests
  // ---------------------------------------------------------------------------
  testPathIgnorePatterns: [
    '<rootDir>/node_modules/',
    '<rootDir>/e2e/',
    '<rootDir>/test/',
    '<rootDir>/src/**/*.e2e-spec.ts',
    '<rootDir>/src/**/*.integration-spec.ts',
  ],

  // Only match unit spec files
  testRegex: '.*\\.spec\\.ts$',

  // ---------------------------------------------------------------------------
  // Performance optimizations for CI
  // ---------------------------------------------------------------------------
  maxWorkers: '50%',
  bail: 1,
  verbose: false,
};

export default config;
<!-- ENDIF -->
