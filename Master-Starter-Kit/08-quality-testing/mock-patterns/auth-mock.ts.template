/**
 * Mock Pattern — Authentication Context / Hook
 *
 * Provides a pre-configured mock user and auth state.
 * Import this file in tests that require an authenticated user.
 *
 * Customize {{AUTH_HOOK_PATH}} to point to your auth hook/context module.
 *
 * Generated by New-Project-Starter-Kit
 */

// ============================================================
// TEST RUNNER: This file uses Jest APIs. For Vitest, replace:
//   jest.fn()          → vi.fn()
//   jest.mock()        → vi.mock()
//   jest.spyOn()       → vi.spyOn()
//   jest.setTimeout()  → vi.setConfig({ testTimeout: N })
//   jest.clearAllMocks()   → vi.clearAllMocks()
//   jest.restoreAllMocks() → vi.restoreAllMocks()
//   jest.useFakeTimers()   → vi.useFakeTimers()
//   @jest/globals import   → import { vi } from 'vitest'
// Vitest is API-compatible — search-replace jest→vi covers most cases.
// ============================================================

// =============================================================================
// Mock user
// =============================================================================
export const mockUser = {
  id: 'test-user-id',
  email: 'test@example.com',
  name: 'Test User',
  role: 'admin',
  tenantId: 'test-tenant-id',
};

// =============================================================================
// Mock auth state
// =============================================================================
export const mockAuth = {
  user: mockUser,
  isAuthenticated: true,
  isLoading: false,
  login: jest.fn(),
  logout: jest.fn(),
  getToken: jest.fn().mockReturnValue('test-token'),
};

// =============================================================================
// Auto-mock the auth hook/context
// =============================================================================
jest.mock('{{AUTH_HOOK_PATH}}', () => ({
  useAuth: () => mockAuth,
}));
