---
id: KF-{{PROJECT_SHORT}}-{{SEQ}}
category: # gotcha | template | process | generator | placeholder | anti-pattern
severity: # critical | high | medium | low
tech_stack: [] # e.g., [next.js, drizzle, stripe]
source_file: # project file that spawned this candidate
kit_target: # where in the kit this should land
sanitized: true
status: pending # pending | exported | applied-upstream
date: {{DATE}}
---

## Summary

<!-- One-line description of the improvement. Be specific enough that someone reading the harvest queue can decide whether to apply it without reading the full context. -->

## Context (sanitized)

<!-- What happened in the project that surfaced this. All project-specific details must be stripped:
- NO project names, client names, company names
- NO domain names, URLs, IP addresses
- NO API keys, tokens, credentials
- NO business-specific terminology
- YES technology names, error messages, version numbers
- YES generalizable patterns and code snippets (with names genericized)

Example: "While implementing auth with Better Auth v1.2, discovered that the default session table requires an explicit `expiresAt` column or silent auth failures occur." -->

## Improvement

<!-- The actual content to add or change in the kit. This should be ready to apply as-is.

For gotchas: Write the gotcha entry exactly as it should appear in the target lessons file.
For templates: Show the diff or replacement content.
For anti-patterns: Write the full anti-pattern entry (pattern, prevention, detection).
For process changes: Describe the process change and where it applies.
For generators: Show the improved prompt or criteria.
For placeholders: Show the placeholder name, description, and resolution rule. -->

## Suggested Kit Location

<!-- Where in the kit this should land, with rationale.

Example: "13-lessons-gotchas/better-auth-gotchas.md -- This is a technology-specific gotcha that applies to any project using Better Auth, not specific to any one project's implementation." -->
