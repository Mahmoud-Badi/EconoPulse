{
  "$schema": "Kit Feedback Hook Configuration",
  "$description": "Add these hooks to your project's .claude/settings.json to enable automatic feedback capture and export. The Stop hook auto-exports pending candidates and commits them to git on every session end. No manual /kit-feedback commands needed.",
  "$install": "Copy the 'hooks' object below into your .claude/settings.json, or merge into existing hooks.",

  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash tools/hooks/kit-feedback-auto-export.sh",
            "timeout": 10000
          }
        ]
      }
    ]
  }
}
